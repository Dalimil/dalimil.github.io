{
  "name": "dalimil.github.io",
  "version": "1.0.0",
  "private": true,
  "author": "Dalimil Hajek",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && node afterbuild.js && rm ./build/static/css/*.map && rm ./build/static/js/*.map && rm -rf ../static/ && cp -r ./build/* ../ && rm -r ./build"
  },
  "devDependencies": {
    "react-scripts": "^0.8.5"
  },
  "dependencies": {
    "jquery": "^3.5.0",
    "particles.js": "^2.0.0"
  }
}
